---
Note: you classified 0 of {len(by_label)} candidates as keep. If truly none apply, return an empty `keeps` list — otherwise reconsider and keep the chunks whose parent system scope governs the sub-query.
